# Prod-mode backend URL (08.09 follow-up — proper dev/prod extension environments). Loaded by
# WXT's own per-mode env mechanism whenever mode is "production" — which `wxt build`/`wxt zip`
# use by default (see .env.development's own comment for the full mechanism). PLACEHOLDER: the
# real production backend doesn't exist yet — replace this one line with the actual deployed
# backend's URL (scheme + host, no trailing slash, matching BACKEND_URL's existing shape in
# lib/backend.ts) once it does. Nothing else needs to change — wxt.config.ts's manifest()
# function and lib/backend.ts both read this same value.
WXT_BACKEND_URL=https://REPLACE_WITH_PROD_BACKEND_URL
